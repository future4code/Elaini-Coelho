```
function contaOcorrencias(arrayDeNumeros, numeroEscolhido) {
  
  for (elemento of arrayDeNumeros) {
    while(elemento) {
      case 1:
        numeroEscolhido === arrayDeNumeros[0]
        return` O número ${numeroEscolhido} aparece ${arrayDeNumeros}x`
    
      default {
        return `Não encontrado`
      }
    }
  }
}
```